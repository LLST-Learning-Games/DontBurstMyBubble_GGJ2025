using UnityEngine;

public class DarrenTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        var greeting = "Hello World!";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
